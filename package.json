{
  "name": "zlib.es",
  "version": "0.1.0",
  "description": "ECMAScript compliant lightweight zlib implementation",
  "main": "dist/cjs/zlib.js",
  "module": "dist/esm/zlib.mjs",
  "types": "dist/tsc/zlib.d.ts",
  "scripts": {
    "test": "mocha ./test/index.js",
    "build": "npm run lint && npm run build:tsc && npm run build:rollup && npm run build:uglify && npm run test",
    "debug": "npm run build:tsc && npm run build:rollup && npm run test",
    "build:tsc": "tsc",
    "build:rollup": "rollup -c",
    "build:uglify": "uglifyjs dist/browser/zlib.js -o dist/browser/zlib.min.js -cm --source-map",
    "lint": "tslint --fix './src/**/*.ts'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zprodev/zlib.es.git"
  },
  "author": "zprodev <zproject.develop@gmail.com> (https://github.com/zprodev)",
  "license": "Zlib",
  "bugs": {
    "url": "https://github.com/zprodev/zlib.es/issues"
  },
  "files": [
    "README.md",
    "dist"
  ],
  "homepage": "https://github.com/zprodev/zlib.es#readme",
  "keywords": [
    "zlib"
  ],
  "devDependencies": {
    "mocha": "^5.2.0",
    "rollup": "^0.65.2",
    "tslint": "^5.11.0",
    "typescript": "^3.0.3",
    "uglify-es": "^3.3.9"
  }
}
